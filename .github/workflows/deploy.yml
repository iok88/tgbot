script: |
  cd /root/iok/tgbot/tgbot
  git fetch origin
  git reset --hard origin/main

  # ставим зависимости в venv
  source venv/bin/activate
  pip install -r requirements.txt

  sudo systemctl restart tgbot